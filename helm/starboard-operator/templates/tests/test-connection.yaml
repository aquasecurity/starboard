apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "starboard.fullname" . }}-test-connection"
  labels:
    {{- include "starboard.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "starboard.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
